en:
  hello: "Hello world"

  errors:
    messages:
      invalid_credentials: "Invalid email or password"

  activerecord:
    errors:
      models:
        user:
          attributes:
            name:
              blank: "Handle name is required"
              too_long: "Handle name must be %{count} characters or less"
            email:
              blank: "Email is required"
              invalid: "Invalid email address"
              taken: "This email address is already registered"
            password:
              blank: "Password is required"
              too_short: "Password must be at least %{count} characters"
              invalid: "Password must contain only alphanumeric characters"
            password_confirmation:
              confirmation: "Password confirmation doesn't match"
    attributes:
      user:
        name: "Handle name"
        email: "Email"
        password: "Password"
        password_confirmation: "Password confirmation"
        bio: "Bio"
