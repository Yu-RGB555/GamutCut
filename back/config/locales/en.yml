en:
  hello: "Hello world"

  errors:
    messages:
      invalid_credentials: "Invalid email or password"

  activerecord:
    errors:
      models:
        user:
          attributes:
            name:
              blank: " is required"
              too_long: " must be %{count} characters or less"
            email:
              blank: " is required"
              invalid: " is invalid"
              taken: " is already registered"
            password:
              blank: " is required"
              too_short: " must be at least %{count} characters"
              invalid: " must contain only alphanumeric characters"
            password_confirmation:
              confirmation: " doesn't match"
        work:
          attributes:
            title:
              blank: " is required"
              too_long: " must be %{count} characters or less"
            description:
              too_long: " must be %{count} characters or less"
            illustration_image:
              blank: " is required"
              too_large: " file size must be %{capacity}MB or less"
            set_mask_data:
              blank: " is required"
    attributes:
      user:
        name: "Handle name"
        email: "Email"
        password: "Password"
        password_confirmation: "Password confirmation"
        bio: "Bio"
